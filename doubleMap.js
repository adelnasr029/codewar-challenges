function double(array) {
    // arr of intger
    // new arr where each el is double to its corresponding in the given arr
    // 
      //loop use map summing each el with itself or multiply it by 2
      return array.map(el => el * 2)
    }